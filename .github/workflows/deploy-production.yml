name: Deploy Web Production

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vercel-production
  cancel-in-progress: false

jobs:
  deploy-production:
    name: Build and deploy to Vercel
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install --global vercel@56.2.0

      - name: Pull production configuration
        run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Deploy production
        # Build on Vercel so production env vars are injected there and do not
        # need to be duplicated in GitHub. Archive the source to stay below the
        # per-deploy file limit.
        run: vercel deploy --prod --archive=tgz --token="$VERCEL_TOKEN"
